<view class="empty" wx:if="{{ tests == 0 }}">
	<view>
		<view class="png">
    		<image mode="aspectFit" src="/static/img/default.png"></image>
    	</view>
    	<view class="title">{{ title }}</view>
    </view>
</view>